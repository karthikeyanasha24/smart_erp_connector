# Copy to .env and fill in values. Do not commit .env.

# Google Drive export (Web application OAuth client ID only — no secret needed in the browser)
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional: backend URL when not using Vite proxy (production)
# VITE_API_URL=https://smart-erp-backend-aa9k.onrender.com
